import { Flex, FlexProps } from 'components/Layout'
import styled from 'styled-components'

export const HeaderStyled = styled((props: FlexProps) => <Flex {...props} />)`
    height: 56px;
    padding: 0 16px;
    background-color: #0f0f0f; //C!!
`
